; PHP's own gzip compression can double up with the webserver/CDN's own
; compression on some hosts, corrupting larger responses (browsers see this
; as ERR_CONTENT_DECODING_FAILED). Let the server/CDN handle compression
; alone.
zlib.output_compression = Off
